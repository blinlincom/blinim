name: imblinlin
version: 0.1.0+1
publish_to: 'none'
description: Blinlin community + WuKongIM realtime chat Flutter application.

environment:
  sdk: ^3.9.0

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  http: ^1.2.2
  shared_preferences: ^2.3.3
  intl: ^0.20.1
  cached_network_image: ^3.4.1
  flutter_staggered_grid_view: ^0.7.0
  crypto: ^3.0.6
  encrypt: ^5.0.3
  wukongimfluttersdk: ^1.7.9
  video_player: ^2.9.2
  file_picker: ^8.1.7

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
